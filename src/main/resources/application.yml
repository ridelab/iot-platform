spring:
  application:
    name: iot-platform
  main:
    banner-mode: 'off'
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8080

logging:
  level:
    org.sselab: trace
